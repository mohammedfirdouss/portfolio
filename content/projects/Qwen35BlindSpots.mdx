---
title: Qwen3.5-4B Base Blind Spots
description: A careful evaluation framework that tests Qwen3.5-4B-Base with 84 structured prompts in 12 reasoning categories. It labels failures on two axes and found a critical float16 overflow issue.
date: "2026-06-08"
repository: mohammedfirdouss/blind-spots-of-frontier-models
published: true
outcomes:
  - Found a critical float16 numerical overflow that makes output unreadable across all 12 categories. bfloat16 is required for valid evaluation of Qwen-class models.
  - Measured failure rates per category using 84 prompts (5 failure probes + 2 success controls per category), giving numbers instead of one-off examples.
  - Gave concrete fine-tuning advice, with specific datasets and scale ranges, mapped to each root cause.
roleHighlights:
  - I designed the 84-prompt test suite and a two-axis labelling scheme that separates coherence failures from correctness failures.
  - I found and documented the float16 overflow issue as a repeatable, measured finding across all 12 reasoning categories.
  - I built a training dataset with per-dtype outputs and auto-labels, published to Hugging Face for reuse.
proofLinks:
  - label: Repository
    href: https://github.com/mohammedfirdouss/blind-spots-of-frontier-models
  - label: Dataset
    href: https://huggingface.co/datasets/mohammedfirdouss/qwen35-4b-base-blind-spots
  - label: Model tested
    href: https://huggingface.co/Qwen/Qwen3.5-4B-Base
---

Qwen3.5-4B Base Blind Spots is an evaluation framework that finds where Qwen3.5-4B-Base fails. The model is a 4B-parameter pre-trained model with Gated DeltaNet attention and a sparse MoE architecture. Instead of collecting one-off failure examples, the framework runs 84 structured prompts across 12 reasoning categories and reports measured failure rates with clear paths to fix them.

## What it is

A repeatable LLM failure-mode evaluation framework with:

- **84-Prompt Test Suite:** 5 failure probes + 2 success controls per category across 12 reasoning domains: arithmetic, logic, commonsense, coreference, Bayesian reasoning, Winograd schema, and more.
- **Two-Axis Labelling:** Each output is labelled separately for coherence (is it readable?) and correctness (is it accurate?). This separates format failures from reasoning failures, which makes fixes more targeted.
- **Critical dtype Finding:** float16 causes numerical overflow across all 12 categories, producing unreadable output even on simple controls. bfloat16 is required; the float16 results are kept as a reference.
- **Fine-Tuning Roadmap:** Failure analysis per category maps each root cause to specific training datasets and scale ranges.

## How It's Built

- **Model:** `Qwen/Qwen3.5-4B-Base` via Hugging Face Transformers.
- **Execution:** `colab_notebook.ipynb` (Google Colab L4/A10G, bfloat16) or `modal_runner.py` for cloud GPU.
- **Decoding:** Greedy sampling for reproducibility.
- **Data Format:** JSONL: `prompts.jsonl` (84 canonical inputs), `train.jsonl` (full records with per-dtype outputs and auto-labels), `blind_spots_data.jsonl` (labelled results).
- **Published Artifact:** Training dataset released on Hugging Face for reuse and extension.

## What I Learned

- **dtype Is a Hard Correctness Requirement:** float16 overflow is not a small quality drop; it makes all results invalid. Check the dtype before trusting any evaluation.
- **Structured Probing Over One-Off Examples:** Controlled failure/success pairs per category turn vague quality concerns into failure rates you can measure and compare.
- **Two-Axis Labels Point to Different Fixes:** Coherence failures point to inference config or instruction tuning. Correctness failures point to chain-of-thought or domain-specific data. Mixing them up wastes effort.
- **Small Benchmarks Can Be High Signal:** When the goal is targeted fine-tuning, 84 prompts across 12 categories surface repeat blind spots faster than large evaluation suites.
